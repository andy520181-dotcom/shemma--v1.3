# 🔧 快速修复：应用名称国际化

## 问题总结
1. ✅ **语言切换按钮** - 已修复（改为显示目标语言）
2. ⏳ **应用名称显示 "app"** - 需要将 InfoPlist.strings 添加到 Xcode 项目

---

## 解决方案：在 Xcode 中添加 InfoPlist.strings

### 方法一：拖拽添加（最简单）⭐

1. **打开 Finder 和 Xcode 并排显示**
   - 左侧：Finder，打开路径 `/Users/andy/Desktop/Matoto马吐吐/ios/App/App`
   - 右侧：Xcode，打开项目

2. **拖拽文件夹到 Xcode**
   - 在 Finder 中选中这两个文件夹：
     - `zh-Hans.lproj`
     - `en.lproj`
   - 拖拽到 Xcode 左侧的 **App** 文件夹下

3. **在弹出对话框中设置**
   ```
   Destination:
   ☐ Copy items if needed (不勾选)
   
   Added folders:
   ⦿ Create groups (选这个！重要)
   ○ Create folder references
   
   Add to targets:
   ☑ App (确保勾选)
   ```
   - 点击 **Finish**

4. **验证添加成功**
   - 在 Xcode 左侧应该看到：
     ```
     App/
     ├── zh-Hans.lproj/
     │   └── InfoPlist.strings
     └── en.lproj/
         └── InfoPlist.strings
     ```

5. **配置文件本地化**
   - 点击任一 `InfoPlist.strings` 文件
   - 右侧 File Inspector 中找到 **Localization**
   - 应该看到：
     ```
     ☑ English
     ☑ Chinese (Simplified)
     ```
   - 如果没有，点击 **Localize...** 按钮设置

6. **重新构建**
   - Clean Build Folder (Shift+Cmd+K)
   - Build (Cmd+B)

---

### 方法二：使用菜单添加

1. **在 Xcode 中右键点击 App 文件夹**
   - 选择：**Add Files to "App"...**

2. **选择文件夹**
   - 导航到：`/Users/andy/Desktop/Matoto马吐吐/ios/App/App`
   - 按住 Command 键，同时选中：
     - `zh-Hans.lproj`
     - `en.lproj`

3. **确认选项**
   ```
   Options:
   ☐ Copy items if needed
   ⦿ Create groups (重要！)
   ☐ Create folder references
   
   Add to targets:
   ☑ App
   ```

4. **点击 Add**

---

## 验证应用名称国际化

### 测试步骤：

1. **确保项目已重新构建**
   - Clean Build Folder
   - Build
   - 将更改同步到设备：
     ```bash
     cd "/Users/andy/Desktop/Matoto马吐吐"
     npm run build
     npx cap sync ios
     ```

2. **在 iOS 设备/模拟器上测试**
   
   **中文环境：**
   - 设置 → 通用 → 语言与地区
   - 确保系统语言是"简体中文"
   - 返回主屏幕
   - 应用名称应显示：**马吐吐** ✓

   **英文环境：**
   - 设置 → 通用 → 语言与地区  
   - 将系统语言改为"English"
   - 返回主屏幕
   - 应用名称应显示：**Matoto** ✓

3. **测试应用内语言切换**
   - 打开应用
   - 中文系统下，右上角按钮应显示：**EN**（点击将切换到英文）
   - 英文系统下，右上角按钮应显示：**中**（点击将切换到中文）

---

## 常见问题

### Q: 添加文件后仍显示 "app"？
**解决方法：**
1. 完全删除设备上的应用
2. 在 Xcode 中 Clean Build Folder
3. 删除 DerivedData：
   ```bash
   rm -rf ~/Library/Developer/Xcode/DerivedData/App-*
   ```
4. 重新构建并安装
5. 重启设备

### Q: 文件添加后在 Xcode 中看不到
**检查点：**
- 确保选择了 "Create groups" 而不是 "Create folder references"
- 文件应该显示为黄色文件夹图标（groups）
- 如果是蓝色文件夹图标（folder references），删除并重新添加

### Q: InfoPlist.strings 没有本地化选项
**解决方法：**
1. 确保在 PROJECT → App → Info → Localizations 中已添加：
   - English
   - Chinese (Simplified)
2. 选中 InfoPlist.strings 文件
3. File Inspector → 点击 **Localize...** 按钮
4. 选择语言并确认

---

## 文件内容验证

### zh-Hans.lproj/InfoPlist.strings（应该包含）：
```
"CFBundleDisplayName" = "马吐吐";
"CFBundleName" = "马吐吐";
"NSCameraUsageDescription" = "需要访问相机以拍摄头像照片";
"NSPhotoLibraryAddUsageDescription" = "需要访问相册以保存和选择头像照片";
"NSPhotoLibraryUsageDescription" = "需要访问相册以选择头像照片";
```

### en.lproj/InfoPlist.strings（应该包含）：
```
"CFBundleDisplayName" = "Matoto";
"CFBundleName" = "Matoto";
"NSCameraUsageDescription" = "Camera access is needed to take avatar photos";
"NSPhotoLibraryAddUsageDescription" = "Photo library access is needed to save and select avatar photos";
"NSPhotoLibraryUsageDescription" = "Photo library access is needed to select avatar photos";
```

---

## 当前进度

### ✅ 已完成
- [x] 创建中英文 InfoPlist.strings 文件
- [x] 修复 Storyboard 编译错误
- [x] 修复语言切换按钮显示逻辑
- [x] 优化英文启动页文案

### ⏳ 待完成
- [ ] 在 Xcode 中添加 InfoPlist.strings 文件引用
- [ ] 重新构建项目
- [ ] 测试应用名称国际化

---

**下一步：** 按照上述"方法一"或"方法二"在 Xcode 中添加文件，然后重新构建测试！
