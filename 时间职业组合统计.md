# 时间标签 + 职业标签组合统计报告

## 周一 (monday) 结果统计

根据数据库分析，周一相关的马只有**3个**：

| ID | 马名 | 副标题 | 职业标签 |
|----|------|--------|---------|
| GEN_MON_001 | 开工马 | 开工大吉，马上有钱 | programmer, designer, pm_operator, marketer, engineer, professional, manager, general (全部) |
| GEN_MON_002 | 一马 | 周一不想上班的牛马。 | programmer, designer, pm_operator, marketer, engineer, professional, manager, general (全部) |
| PRO_MON_002 | 梦游马 | 每周一开晨会，身体在工区，大脑还在梦游。 | programmer, designer, pm_operator, engineer, professional, manager, general (7个) |

### 周一 + 各职业标签匹配数量

| 职业 | 可匹配结果数 | 说明 |
|------|-------------|------|
| programmer | **3个** | 开工马、一马、梦游马 |
| designer | **3个** | 开工马、一马、梦游马 |
| pm_operator | **3个** | 开工马、一马、梦游马 |
| marketer | **2个** | 开工马、一马 |
| engineer | **3个** | 开工马、一马、梦游马 |
| professional | **3个** | 开工马、一马、梦游马 |
| manager | **3个** | 开工马、一马、梦游马 |
| general | **3个** | 开工马、一马、梦游马 |

## 问题分析

### 🚨 严重的多样性问题

**周一 + 程序员：只有3个结果**
- 用户连续测试3次后，**100%会遇到重复结果**
- 第4次测试必定重复之前的结果
- 用户体验极差

### 💡 解决方案对比

#### 方案A：连续3次后显示通用结果（当前计划）
- ❌ **问题**：周一的general标签也只有3个结果
- ❌ 切换到general池没有增加多样性
- **结论**：此方案对周一无效

#### 方案B：扩大匹配范围
- 连续3次后，忽略timeTag，只匹配jobTag
- 程序员总共有30个结果，大幅增加多样性
- ✅ **推荐此方案**

#### 方案C：增加数据库条目
- 为周一添加更多专属的马
- 需要创作内容，工作量大
- 长期方案

## 推荐实施方案（方案B）

### 核心逻辑

```typescript
function findCandidates(timeTag: string, jobTag: string, usageCount: number): HorseEntry[] {
  
  // 连续使用3次及以上：忽略时间标签，扩大匹配池
  if (usageCount >= 3) {
    console.log('🎯 多样性模式：忽略时间标签，只匹配职业');
    
    // 只根据职业标签匹配
    const expanded = HORSES_DATA.filter(h => h.jobTag.includes(jobTag));
    if (expanded.length > 0) return expanded;
    
    // 降级到全部结果
    return HORSES_DATA;
  }
  
  // 前2次：正常三层匹配
  // Layer 1: 精确匹配 (时间 + 职业)
  const layer1 = HORSES_DATA.filter(h =>
    matchTime(h.timeTag, timeTag) && h.jobTag.includes(jobTag)
  );
  if (layer1.length > 0) return layer1;
  
  // Layer 2: 时间 + general
  const layer2 = HORSES_DATA.filter(h =>
    matchTime(h.timeTag, timeTag) && h.jobTag.includes('general')
  );
  if (layer2.length > 0) return layer2;
  
  // Layer 3: 全部general
  return HORSES_DATA.filter(h => h.jobTag.includes('general'));
}
```

### 效果对比

**周一 + 程序员：**
- 前2次：3个结果（开工马、一马、梦游马）
- 第3次起：**30个结果**（所有programmer标签的马）
- 多样性提升：**10倍**

## 其他时间标签统计

需要检查其他时间标签（workday_normal, friday_evening等）的结果数量，确认是否也存在类似问题。

**建议：**
全面统计所有时间标签的结果数量，找出结果数不足的时间段。
