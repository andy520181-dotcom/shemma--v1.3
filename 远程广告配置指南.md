# 远程广告配置指南

## 概述

本应用支持远程广告配置，无需更新App即可更换广告内容。

## 配置文件格式

配置文件为JSON格式，包含以下字段：

```json
{
  "enabled": true,
  "imageUrl": "https://cdn.example.com/ad.jpg",
  "duration": 5,
  "skipAfter": 2,
  "link": "https://example.com",
  "validUntil": "2026-12-31"
}
```

### 字段说明

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `enabled` | boolean | ✅ | 是否启用广告，设为`false`可远程关闭 |
| `imageUrl` | string | ✅ | 广告图片URL，建议尺寸1080x1920或同比例 |
| `duration` | number | ✅ | 显示时长（秒），建议3-5秒 |
| `skipAfter` | number | ✅ | 跳过按钮延迟显示（秒），建议2秒 |
| `link` | string | ❌ | 点击跳转URL（可选） |
| `validUntil` | string | ❌ | 有效期（YYYY-MM-DD格式，过期自动不显示） |

## 部署方式

### 方案1：GitHub Pages（推荐）

1. 在当前仓库创建广告配置文件：
   ```bash
   # 创建 gh-pages 分支
   git checkout --orphan gh-pages
   
   # 创建配置文件
   echo '{
     "enabled": true,
     "imageUrl": "https://your-cdn.com/ad.jpg",
     "duration": 5,
     "skipAfter": 2
   }' > ad-config.json
   
   # 提交并推送
   git add ad-config.json
   git commit -m "Add ad config"
   git push origin gh-pages
   ```

2. 启用GitHub Pages：
   - 进入仓库Settings → Pages
   - Source选择`gh-pages`分支
   - 保存后获得URL：`https://andy520181-dotcom.github.io/shemma--v1.3/ad-config.json`

3. 更新代码中的配置URL（如果不同）：
   ```typescript
   // services/adService.ts
   const AD_CONFIG_URL = 'https://andy520181-dotcom.github.io/shemma--v1.3/ad-config.json';
   ```

### 方案2：CDN/云存储

- 上传到阿里云OSS/腾讯云COS
- 配置CORS允许跨域访问
- 使用CDN加速

## 更换广告

只需更新远程配置文件即可，用户下次启动时自动获取新配置：

```bash
# 修改配置文件
vi ad-config.json

# 提交更新
git add ad-config.json
git commit -m "Update ad"
git push origin gh-pages
```

**无需用户更新App！**

## 关闭广告

最简单的方式：

```json
{
  "enabled": false,
  "imageUrl": "",
  "duration": 0,
  "skipAfter": 0
}
```

## 图片要求

- **尺寸**：建议1080x1920（9:16比例）
- **格式**：JPG/PNG
- **大小**：建议压缩到200KB以内
- **内容**：确保重要信息在安全区域内（避开刘海屏/顶部/底部）

## 测试

1. **本地测试**：
   ```bash
   npm run dev
   ```
   使用`public/ad-config.json`进行测试

2. **线上测试**：
   修改配置URL为线上地址，重启App测试

## 缓存机制

- 配置会缓存到LocalStorage，有效期24小时
- 每次启动如果缓存未过期，使用缓存同时后台更新
- 网络失败时使用缓存配置

## 故障降级

- 配置加载失败 → 使用缓存
- 无缓存 → 跳过广告
- 图片加载失败 → 3秒后自动跳过
- `enabled: false` → 跳过广告

## 注意事项

1. ✅ 确保图片URL可访问且支持CORS
2. ✅ 定期检查广告过期时间
3. ✅ 建议使用CDN加速图片加载
4. ✅ 测试不同网络环境下的体验
